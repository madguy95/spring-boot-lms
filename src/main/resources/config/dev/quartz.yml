# ========================================
# DEV - QUARTZ CONFIGURATION
# ========================================
# Quartz is managed by QuartzConfig (not Spring Boot auto-config)
# Enable via: QUARTZ_ENABLED=true
