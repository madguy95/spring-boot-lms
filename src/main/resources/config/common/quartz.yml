# ========================================
# COMMON QUARTZ CONFIGURATION
# ========================================
# Shared Quartz settings across all environments
# Note: QuartzAutoConfiguration is excluded — Quartz is managed entirely by
# QuartzConfig (conditional on app.quartz.enabled=true via QUARTZ_ENABLED env var)
